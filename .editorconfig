root = true

[*]
indent_style = tab
indent_size = 4
end_of_line = lf
charset = utf-8
trim_trailing_whitespace = true
max_line_length = off

# YAML, for whatever god-forsaken reason, doesn't allow hard tabs
[*.{yaml,yml}]
indent_style = space
